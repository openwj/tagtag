<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.tagtag.module.iam.mapper.DeptMapper">
    <!-- 部门分页查询 -->
    <select id="selectPage" resultType="dev.tagtag.module.iam.entity.Dept">
        SELECT id,
               name,
               parent_id   AS parentId,
               sort,
               status,
               create_time AS createTime,
               update_time AS updateTime,
               create_by   AS createBy,
               update_by   AS updateBy
        FROM iam_dept
        <where>
            <if test="q != null">
                <if test="q.name != null and q.name != ''">
                    AND name LIKE CONCAT('%', #{q.name}, '%')
                </if>
                <if test="q.status != null">
                    AND status = #{q.status.code}
                </if>
                <if test="q.parentId != null">
                    AND parent_id = #{q.parentId}
                </if>
            </if>
        </where>

        <choose>
            <when test="orderList != null and orderList.size() &gt; 0">
                ORDER BY
                <foreach collection="orderList" item="sf" separator=",">
                    <choose>
                        <when test="sf.field == 'id'">
                            id
                        </when>
                        <when test="sf.field == 'name'">
                            name
                        </when>
                        <when test="sf.field == 'sort'">
                            sort
                        </when>
                        <when test="sf.field == 'createTime' or sf.field == 'create_time'">
                            create_time
                        </when>
                        <otherwise>
                            id
                        </otherwise>
                    </choose>
                    <choose>
                        <when test="sf.asc == null or sf.asc">
                            ASC
                        </when>
                        <otherwise>
                            DESC
                        </otherwise>
                    </choose>
                </foreach>
            </when>
            <otherwise>
                ORDER BY id ASC
            </otherwise>
        </choose>
    </select>

    <!-- 查询部门的父ID -->
    <select id="selectParentId" resultType="long">
        SELECT parent_id
        FROM iam_dept
        WHERE id = #{id}
    </select>
</mapper>
