## 修复内容
1. 更正 V1 脚本的语法错误：把 `menu_type TINYNT(1)` 改为 `menu_type TINYINT(1)`，并快速复核其余注释与类型。
2. 清理开发库状态（二选一）：
- 方式 A（推荐）：重建库
  - `DROP DATABASE <你的库名>;`
  - `CREATE DATABASE <你的库名> CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;`
- 方式 B：清理历史与半成品
  - `DROP TABLE IF EXISTS flyway_schema_history;`
  - `DROP TABLE IF EXISTS iam_user, iam_role, iam_menu, iam_dept, iam_user_role, iam_role_menu;`
3. 重新构建并启动：
- 打包：`mvn -DskipTests -pl tagtag-start -am package`
- 启动：`java -jar tagtag-start/target/tagtag-start-1.0.0.jar`
- 观察 Flyway 按 `V1 → V2` 执行，应用正常启动。

## 配置确认
- 保留：`spring.flyway.enabled: true`、`spring.flyway.locations: classpath:db/migration`、`spring.flyway.baseline-on-migrate: false`、`spring.flyway.clean-disabled: false`
- 移除：任何 `spring.flyway.errorOverrides`、`spring.flyway.clean-on-validation-error`

## 验证
- 登录：`admin / password`
- 菜单与角色接口正常；授权命中 `PERM_user:create` 返回 200。

我将先修正 V1 脚本并重建项目，你执行数据库清理（A 或 B）。完成后我会再次构建并验证启动日志。