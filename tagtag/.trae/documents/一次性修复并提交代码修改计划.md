## 需要实施的代码与脚本修复
1. 修正 V1 初始化脚本语法错误（根因）
- 文件：`tagtag-module/tagtag-module-iam/src/main/resources/db/migration/iam/V1__init.sql`
- 变更：将 `menu_type TINYNT(1) NOT NULL DEFAULT 1 COMMENT '菜单类型：0目录/1菜单/2按钮'` 更正为 `menu_type TINYINT(1) NOT NULL DEFAULT 1 COMMENT '菜单类型：0目录/1菜单/2按钮'`
- 同时全局搜索替换 `TINYNT(1)` → `TINYINT(1)`，确保无遗漏。

2. 复核并保持其他表与字段备注无误（已按你的要求补充）
- 表备注：`iam_user/iam_role/iam_menu/iam_dept/iam_user_role/iam_role_menu`
- 字段备注：`username/password/menu_code/menu_type/...` 等，保持与业务一致。

3. 构建与清理
- 清理构建产物：`mvn clean -pl tagtag-module-iam,tagtag-start -am`
- 重新打包：`mvn -DskipTests -pl tagtag-start -am package`

4. 清库重建（仅开发库）
- 推荐方式 A：
  - `DROP DATABASE <你的库名>;`
  - `CREATE DATABASE <你的库名> CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;`
- 或方式 B：
  - `DROP TABLE IF EXISTS flyway_schema_history;`
  - `DROP TABLE IF EXISTS iam_user, iam_role, iam_menu, iam_dept, iam_user_role, iam_role_menu;`

5. 启动与验证
- 启动：`java -jar tagtag-start/target/tagtag-start-1.0.0.jar`
- 观察 Flyway 日志应按 `V1 → V2` 执行成功；随后自测：
  - 登录 `admin / password`
  - 菜单与角色接口正常返回
  - 授权命中 `@PreAuthorize("hasAuthority('PERM_user:create')")` 返回 200

## 说明
- 这次将直接修改脚本源文件并提交上述变更；之后进行构建与启动验证，避免再次出现旧构建产物中的 `TINYNT` 残留。
- 不会改动其他业务代码；仅在发现编译或运行异常时进行最小必要的修复。

确认后我立即执行代码修改与构建、清库并启动验证。