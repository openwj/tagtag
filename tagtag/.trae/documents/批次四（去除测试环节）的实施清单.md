## 变更确认
- 去掉 MockMvc 集成测试补充环节，仅做代码优化与编译运行验证。

## 待执行优化（保留分页 XML，不改动复杂 SQL）
- Role 简单查询：
  - 在 `RoleServiceImpl` 增加按 `code/name` 获取单条和唯一性校验的 LambdaQuery 方法
  - 如存在对应 XML 简单查询，改为服务层 LambdaQuery 并删除未使用 XML/Mapper 方法
- Menu 简单查询：
  - 在 `MenuServiceImpl` 增加按 `parentId` 列表和按 `menuCode` 单条的 LambdaQuery 方法
  - 如存在对应 XML 简单查询，改为服务层 LambdaQuery 并删除未使用 XML/Mapper 方法
- Dept 简单查询：
  - 保持分页 XML；如有按父 ID 的简单列表入口，统一为 LambdaQuery（树查询已为 LambdaQuery）

## 统一与清理
- 统一使用 `SecurityClaims`/`SecurityMessages`，消除剩余魔法字符串
- 保留已添加的轻量日志（`@Slf4j`）与缓存策略（`roleMenuCodes`/`deptTree`）
- 删除未使用的 XML 语句与 Mapper 方法声明

## 验证
- 仅进行编译打包与基本运行拨测（不新增测试用例），确保行为等价

## 交付
- 以上代码优化与清理，所有方法保留函数级注释；完成后同步改动说明与验证结果