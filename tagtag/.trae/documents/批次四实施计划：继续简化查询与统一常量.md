## 目标
- 保留分页 XML，仅优化简单单表查询为 LambdaQuery
- 统一安全常量与文案使用，补充轻量日志与基础测试

## 查询优化（不动分页 XML）
- Role：新增基于 Lambda 的简单查询/校验方法（如按 `code/name` 获取单条、唯一性校验）；不改现有分页
- Menu：新增按 `parentId` 获取列表、按 `menuCode` 获取单条的 LambdaQuery 方法；不改现有分页
- Dept：如存在简单列表入口（按父 ID），统一 LambdaQuery（树查询已是 LambdaQuery）

## 常量与日志
- 在剩余的认证/服务逻辑抛错处统一使用 `SecurityMessages` 文案；所有 Claims 读取继续使用 `SecurityClaims`
- 为 Role/Menu/Dept 的增删改添加 `@Slf4j` 轻量日志（记录上下文，不含敏感数据）

## 测试
- 增补 MockMvc 用例：登录成功/失败、刷新版本不匹配/注销后旧令牌 401、权限聚合与缓存命中、简单查询行为等价

## 验证
- 全项目编译打包；dev 环境运行拨测确认行为等价

## 交付
- 提交上述代码改动（含函数级注释），并附简要验证说明