## 目标
- 识别并删除明确未使用、已弃用或仅占位的旧代码与杂项文件，减少噪音与维护成本。

## 明确可删（第一批）
- `tagtag-framework/src/main/java/dev/tagtag/framework/util/PageQueries.java`
  - 证据：类与方法标注 `@Deprecated`，且全库无引用；`PageQueries.java:12`、`PageQueries.java:22`。
  - 说明：分页与默认排序已由 `PageQuery` 校验与 XML 默认排序替代，功能冗余。
- `tagtag-framework/src/main/java/dev/tagtag/framework/security/PasswordUtils.java`
  - 证据：全库零引用，仅自身文件；方法如 `matchesBcrypt` 无下游使用；`PasswordUtils.java:41-44`。
  - 说明：当前使用 `PasswordEncoder` 直接校验，工具类冗余。
- `tagtag-framework/src/main/java/dev/tagtag/framework/aspect/RateLimitAspect.java`
  - 证据：仅占位，环绕直接放行，未结合注解/存储实现；`RateLimitAspect.java:28-31`。
  - 说明：保留无实际价值，建议移除或待未来实现再引入。
- `tagtag-framework/src/main/java/dev/tagtag/framework/aspect/OperationLogAspect.java`
  - 证据：仅示例，未输出实际日志；`OperationLogAspect.java:27-30`。
  - 说明：无下游依赖，可删除。
- `tagtag-kernel/src/main/java/dev/tagtag/kernel/util/PermissionChecker.java`
  - 证据：全库零引用；`PermissionChecker.java:37-49`。
  - 说明：已有 `UserContextHolder` 提供同类判断，重复且未使用。
- `tagtag-kernel/src/main/java/dev/tagtag/kernel/util/JwtUtils.java`
  - 证据：全库零引用，现行实现使用 `JwtProvider/JwtService`；`JwtUtils.java:15-33`、`JwtUtils.java:35-50`。

## 杂项清理（建议项）
- `logs/` 运行日志（如 `tagtag-admin.log`、`.gz`）
  - 说明：运行时产物不应提交版本库；建议清理并将 `logs/` 加入 `.gitignore`。
- `.idea/` IDE 配置目录
  - 说明：本地 IDE 元数据，易产生冲突；建议清理并 `.gitignore` 排除。
- 空壳模块：`tagtag-integration/`、`tagtag-test/`
  - 说明：均为 `packaging=pom` 且无源码；若短期不使用，可从父 POM 的 `<modules>` 中移除以简化构建。
- 文档目录：`.trae/documents/`
  - 说明：非代码资源；如团队不需随源码分发，可迁移到文档库。

## 验证步骤
- 本地构建：`mvn -q -DskipTests package`，确保删除后编译通过。
- 运行冒烟：启动 `tagtag-start`，访问基础接口，验证分页、排序、认证与异常处理链路正常。
- 搜索回归：全库 `grep` 检查是否仍引用被删除类名，确保零命中。

## 风险与回滚
- 风险：占位切面删除后不会影响现有功能；如未来需要限流/操作日志，可在实现成熟后重新引入。
- 回滚：基于 Git 可快速恢复；建议以独立提交完成每一类清理，便于回滚。

## 执行顺序
1) 删除 `PageQueries.java`（无引用且弃用）。
2) 删除 `PasswordUtils.java`、`JwtUtils.java`、`PermissionChecker.java`（零引用工具）。
3) 删除占位切面：`RateLimitAspect.java`、`OperationLogAspect.java`。
4) 清理 `logs/`、`.idea/` 并更新 `.gitignore`。
5) 视团队规划移除空壳模块并更新父 POM。

请确认以上方案，我将按“执行顺序”逐项清理并验证。